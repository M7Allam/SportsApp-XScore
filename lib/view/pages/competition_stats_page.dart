import 'package:flutter/material.dart';
import 'package:world_cup_qatar_2022/view/widgets/app_background.dart';

class CompetitionStatsPage extends StatelessWidget {
  const CompetitionStatsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: AppBackground(
        child: SafeArea(
          child: Container(),
        ),
      ),
    );
  }
}
